import 'package:flutter/material.dart';
import 'profilescreen.dart';

class BottomNavPerfilOnly extends StatelessWidget {
  const BottomNavPerfilOnly({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: ProfileScreen(),
    );
  }
}